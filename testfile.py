import sys
#test to commit to remote 

print(sys.path)

print(sys.executable)

# Personal notes To activate virtual invironment: 
# After create the GIT folder, type: "python3 or py -m venv /path/to/new/virtual/environment" Example: py -m venv .venv
# Then to activate virtual env .\.venv\Scripts\activate
# And if it doesn't worl try on VS. "ctrl shift + p in Vscode to pull up settings > Python Interpreter and select the virtual environment"